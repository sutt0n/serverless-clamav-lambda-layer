{
  "name": "aws-ffmpeg-layer",
  "description": "",
  "version": "0.1.0",
  "dependencies": {
    "serverless": "^3.25.0"
  },
  "devDependencies": {
    "aws-sdk": "^2.964.0",
    "jest": "^29.2.2",
    "npm-check": "^6.0.1",
    "prettier": "^2.4.1"
  },
  "scripts": {
    "build": "./build.sh",
    "deploy": "sls deploy",
    "test": "jest handler.test.js",
    "format": "npm run prettier",
    "prettier": "npx prettier --write ./*.js"
  }
}
